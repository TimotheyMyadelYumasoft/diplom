type Props = {
    setIsOpen: (isOpen: boolean) => void;
}

const EditForm = ({ setIsOpen }: Props) => {
    console.log('hey')
    return (
        <div>
            asdf
        </div>
    )
}

export default EditForm;